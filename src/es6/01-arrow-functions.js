// Funcion normal 
function square(num) {
    return num * num;
}
// Funcion flecha con return explicito
const square = (num) => {
    return num * num;
}
// Funcion flecha con return implicito
const square = num => num * num;